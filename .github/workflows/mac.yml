name: Mac runner test

on:
  push:
    paths:
    - '.github/workflows/mac.yml'
  workflow_dispatch:

jobs:
  app-test:
    name: Run Mac test
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
